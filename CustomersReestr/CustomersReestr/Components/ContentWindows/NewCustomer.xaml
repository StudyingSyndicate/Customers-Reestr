<Page x:Class="CustomersReestr.NewCustomer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d">

    <WrapPanel Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" VerticalAlignment="Center">
                <Label Width="100" VerticalContentAlignment="Center">ФИО:</Label>
                <TextBox/>
            </DockPanel>

            <DockPanel Grid.Row="1" VerticalAlignment="Center">
                <Label Width="100" VerticalContentAlignment="Center">Пол:</Label>
                <TextBox/>
            </DockPanel>

            <DockPanel Grid.Row="2" VerticalAlignment="Center">
                <Label Width="100" VerticalContentAlignment="Center">Телефон:</Label>
                <TextBox/>
            </DockPanel>
        </Grid>
    </WrapPanel>
</Page>
